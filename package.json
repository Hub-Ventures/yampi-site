{
  "name": "yampi-sitio-comercial",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "docker compose up -d --build",
    "deploy:docker": "docker compose build"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^6.0.0"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
